# About This Project

This project is done as to fulfill BNCC FrontEnd project requirements. It is done as a proof of learning of the FrontEnd course.

# React + Vite

This project uses React working in Vite with HMR and some ESLint rules.

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances.
